import { AllExceptionsrFilter } from './all-exceptionsr.filter';

describe('AllExceptionsrFilter', () => {
  it('should be defined', () => {
    expect(new AllExceptionsrFilter({} as any, {} as any)).toBeDefined();
  });
});
